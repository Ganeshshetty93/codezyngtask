This folder contains test helpers for AI features.

Run the AI feature test script:

```
cd backend
node scripts/test_ai_features.js
```

Ensure `OPENAI_API_KEY` is set in `backend/.env` before running.
